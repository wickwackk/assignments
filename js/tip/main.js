let une = 3000;

5000 < une && une < 30000
  ? (une = une + (une / 100) * 15)
  : (une = une + (une / 100) * 20);

console.log(une);

une = 27500;

5000 < une && une < 30000
  ? (une = une + (une / 100) * 15)
  : (une = une + (une / 100) * 20);

console.log(une);

une = 100000;

5000 < une && une < 30000
  ? (une = une + (une / 100) * 15)
  : (une = une + (une / 100) * 20);

console.log(une);
