// #1
// "Comments can make code readable"

// #2
// "Javascript lesson begin"

// #3
/*
"Comments can make code readable"
*/

// #6
let n1;

// #7
let n2 = 5;

// #8
let FirstName;
let LastName;
let MaritalStatus;
let Country;
let age;

// #9
let fName, lName, maritalS, cntry, age1;

let MyAge, yourAge;
MyAge = 5;
yourAge = MyAge;

// #10
let aSide, bSide, cSide;
let perimeter = aSide + bSide + cSide;

// #11
let r;
const PI = 3.14;
let s = r * r * PI;

// #12
let y2, y1, x2, x1;
let m = (y2 - y1) / (x2 - x1);

// #13
let b = 1;
let kb = 1000 * b;
let megabyte = 1000 * kb;
let gigabyte = 1000 * megabyte;
console.log(15 * gigabyte);

// #14
let byte = 1;
let kilobyte = 1000 * b;
let mb = 1000 * kb;
let fileSize = 15 * mb;
let transferTime = 3;
let BITrate = (fileSize / transferTime) * 8;

// #15
let side1, side2, side3;
let height = math.sqrt((side2 / 2) * (side2 / 2) - side3 * side3);
let area = (h * side2) / 2;

// #16
let celcius = prompt();
let farenheit = c * 1.8 + 32.0;

// #17
let faren = prompt();
let celc = (f - 32.0) / 1.8;

// #18
let num1 = 15;
let num2 = 10;
let addition = a + b;
let subtraction = a - b;
let multiply = a * b;
let divide = a / b;
let remainder = a % b;
console.log("Addition of given 2 numbers is ", addition);
console.log("Subtraction of given 2 numbers is ", subtraction);

// #19
let centimeter = prompt("cm to inch");
let inches = cm * 0.393701;
alert(inch + " inch");

let inch = prompt("inch to cm");
let cm = inch / 0.393701;
alert(cm + " cm");

// #20
let radius = prompt("enter radius:");
const PiNum = 3.14;
let circum = 2 * PiNum * r;
let circleVolume = (4 / 3) * PiNum * r * r * r;

// #21
let threeDigit = prompt("3n orontoi too oruulna uu:");
let too1 = parseInt(threeDigit / 10);
let too2 = parseInt(too1 / 10);
alert(too2);

// #22
let first = "Baby Shark";
let sec = "Mommy Shark";
let third = "Daddy Shark";
let four = "Grandma Shark";
let five = "Grandpa Shark";
let six = "Let's go hunt";
let seven = "Run away";
let eight = "Safe at last";
let nine = "It's the end";
let main = ", doo-doo, doo-doo";
let ah = " (ah!)";
let phew = " (phew)";

console.log(first + main, "\n", first + main, "\n", first + main, "\n", first);
console.log(sec + main, "\n", sec + main, "\n", sec + main, "\n", sec);
console.log(third + main, "\n", third + main, "\n", third + main, "\n", third);
console.log(four + main, "\n", four + main, "\n", four + main, "\n", four);
console.log(five + main, "\n", five + main, "\n", five + main, "\n", five);
console.log(six + main, "\n", six + main, "\n", six + main, "\n", six);
console.log(
  seven + main,
  "\n",
  seven + main,
  "\n",
  seven + main,
  "\n",
  seven + ah
);
console.log(
  eight + main,
  "\n",
  eight + main,
  "\n",
  eight + main,
  "\n",
  eight + phew
);
console.log(nine + main, "\n", nine + main, "\n", nine + main, "\n", nine);
