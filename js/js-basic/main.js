// "Comments can make code readable"
// "Javascript lesson begin"
/*
"Comments can make code readable"
*/
let num1;
let num2 = 5;
let FirstName;
let LastName;
let MaritalStatus;
let Country;
let age;
// let FirstName, LastName, MaritalStatus, Country, age;

let MyAge, yourAge;
MyAge = 5;
yourAge = MyAge;

//Triangle perimeter
let aSide, bSide, cSide;
let perimeter = aSide + bSide + cSide;

//Surface area of a circle
let r;
const PI = 3.14;
let s = r * r * PI;

// Ilerhiilel
let y2, y1, x2, x1;
let m = (y2 - y1) / (x2 - x1);

// Gigabyte
let megabyte = 1;
let gigabyte = 1000 * megabyte;
console.log(15 * gigabyte);

//BIT rate
let b = 1;
let kb = 1000 * b;
let mb = 1000 * kb;
let fileSize = 15 * mb;
let transferTime = 3;
let BITrate = (fileSize / transferTime) * 8;

// Area of a triangle
let side1, side2, side3;
let h = math.sqrt((side2 / 2) * (side2 / 2) - side3 * side3);
let area = (h * side2) / 2;

//Celcius to Farenheit
let c = prompt();
let f = c * 1.8 + 32.0;

//Farenheit to Celcius
let f = prompt();
let c = (f - 32.0) / 1.8;

//21
let a = 15;
let b = 10;
let addition = a + b;
let subtraction = a - b;
let multiply = a * b;
let divide = a / b;
let remainder = a % b;
console.log("Addition of given 2 numbers is ", addition);
console.log("Subtraction of given 2 numbers is ", subtraction);

//22
let cm = prompt("cm to inch");
let inch = cm * 0.393701;
alert(inch + " inch");

//inch to cm
let inch = prompt("inch to cm");
let cm = inch / 0.393701;
alert(cm + " cm");
